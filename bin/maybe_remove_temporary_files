#!/bin/bash

DIR="/tmp/create_temporary_file"

# TODO This doesn't work correctly when called from .zlogout
if pgrep alacritty; then
    echo "doing nothing"
else
    rm /tmp/create_temporary_file/*

fi
